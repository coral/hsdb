{
  "name": "decks",
  "description": "user account resource",
  "routes": {
    "GET /decks": "index",
    "GET /decks/:deck": "show",
    "GET /decks/text/:deck": "showAsText",
    "POST /decks": "create",
    "PUT /decks/:deck": "update",
    "DELETE /decks/:deck/:card": "deleteCard"
  }
}
